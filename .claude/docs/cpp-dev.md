# C++ Development

| Tool | Config / Notes |
|---|---|
| Formatter | `clang-format` — style at `~/clang-format`; runs on save via Neovim autocmd |
| Build system | CMake — use the `cmake-architect` agent for build config |
| LSP | `clangd` — configured in `~/.config/nvim/lua/core/lsp.lua` |
| Debugger | `gdb` — startup commands in `~/.gdbinit` |
